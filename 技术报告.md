# 竞品洞察智能体技术报告

## 1. 项目概述

本项目名为 **Competitor Insight Agent**，是一个基于大语言模型和在线搜索能力的智能竞品分析工具。用户在网页中输入一个产品或一组产品名称后，系统会自动识别分析主题、补充直接竞品、检索公开网页资料、抽取结构化竞品画像，并最终生成一份中文 Markdown 竞品分析报告。

项目的核心目标不是让大模型凭记忆直接生成结论，而是通过 **Tavily 在线搜索 + Agno 多智能体编排 + DeepSeek 大模型推理 + Pydantic 结构化校验** 的方式，把竞品分析拆成多个可解释、可检查的步骤，从而降低幻觉风险，提高报告的可追溯性。

## 2. 需求分析

竞品分析通常需要完成以下工作：

1. 明确分析对象和对比范围。
2. 搜集官网、定价页、产品文档、新闻、第三方评测、用户反馈等公开资料。
3. 从非结构化网页资料中抽取产品定位、目标用户、核心功能、定价、优劣势等信息。
4. 对多个产品进行横向比较，形成 SWOT、机会点和产品策略建议。
5. 输出结构清晰、便于阅读和下载的分析报告。

如果完全由人工完成，上述流程耗时较长；如果完全依赖大模型直接生成，又容易出现来源不明、事实过时或结论不可验证的问题。因此本项目采用“检索增强生成”的思路：先从真实网页获取证据，再让大模型基于证据完成抽取、对比和写作。

## 3. 技术选型

项目主要技术栈如下：

| 技术 | 作用 |
| --- | --- |
| Python 3.10+ | 项目开发语言 |
| Streamlit | 构建网页交互界面 |
| Agno | 编排多个智能体，管理 Agent 调用流程 |
| DeepSeek | 负责规划、抽取、分析和报告生成等语言推理任务 |
| Tavily Search API | 检索实时公开网页资料 |
| Pydantic | 约束大模型输出格式，进行结构化校验 |
| dataclass | 定义项目内部业务数据对象 |
| JSON 文件 | 缓存竞品画像，减少重复搜索和模型调用 |

其中，Agno 用于把一个复杂任务拆成多个智能体协作完成；Tavily 用于提供外部证据；Pydantic 用于约束大模型输出，避免后续流程收到难以解析的自由文本。

## 4. 项目结构

项目采用 `src` 布局，主要文件如下：

```text
.
├── README.md
├── pyproject.toml
├── streamlit_app.py
├── data/
│   ├── cache/
│   └── reports/
└── src/
    └── competitor_agent/
        ├── agno_workflow.py
        ├── config.py
        ├── models.py
        └── tools/
            ├── cache_tools.py
            ├── real_search_tools.py
            └── report_tools.py
```

各模块职责如下：

- `streamlit_app.py`：项目入口，负责网页界面、API Key 输入、用户请求提交、进度展示和 Markdown 下载。
- `agno_workflow.py`：核心业务流程，创建并调度 Planner、Search、Extractor、Analyst、Reporter 五个智能体。
- `models.py`：定义项目内部数据结构，例如资料来源、竞品画像、分析计划和最终报告。
- `config.py`：集中管理路径、默认模型名、Tavily 搜索参数等配置。
- `real_search_tools.py`：封装 Tavily HTTP 搜索请求，并把原始搜索结果压缩成适合大模型处理的短 JSON。
- `cache_tools.py`：负责竞品画像缓存的读取和写入。
- `report_tools.py`：提供将 Markdown 报告保存到本地的工具函数。

## 5. 总体架构设计

系统整体采用“前端交互层 + 工作流编排层 + 工具层 + 数据层”的结构。

```text
用户
 │
 ▼
Streamlit 网页界面
 │
 ▼
AgnoCompetitorWorkflow
 │
 ├── PlannerAgent：识别主题并确定 3 个并列分析产品
 ├── SearchAgent：调用 Tavily 搜索公开资料
 ├── ExtractorAgent：抽取单个产品的结构化画像
 ├── AnalystAgent：做横向比较、SWOT 和策略分析
 └── ReportAgent：生成中文 Markdown 报告
 │
 ▼
Markdown 报告展示与下载
```

这种设计的优点是每个智能体只负责一个相对明确的子任务，便于控制提示词、输出结构和错误定位。例如，如果结果中产品列表不正确，可以重点检查 PlannerAgent；如果来源不充分，可以重点检查 SearchAgent；如果报告格式不符合要求，可以重点调整 ReportAgent。

## 6. 多智能体工作流实现

核心流程位于 `AgnoCompetitorWorkflow.run()` 方法中。一次完整分析包含以下步骤。

### 6.1 规划阶段

PlannerAgent 接收用户自然语言请求，输出 `AnalysisPlanOutput`。该阶段负责识别：

- 分析主题 `topic`
- 恰好 3 个竞品名称 `competitor_names`
- 分析维度 `analysis_dimensions`
- 所需资料来源 `required_sources`

项目对 PlannerAgent 做了明确约束：如果用户只输入 1 个产品，系统必须通过搜索补充 2 个直接竞品；如果用户输入 2 个产品，则补充 1 个；如果用户已经输入 3 个产品，则不再替换或扩展。这样可以保证后续报告始终围绕 3 个并列产品进行横向对比。

### 6.2 检索阶段

SearchAgent 针对每个产品调用 Tavily 搜索工具，检索官网、定价、产品功能、用户评价等资料。搜索工具在 `real_search_tools.py` 中实现，主要逻辑包括：

- 通过 HTTP POST 请求访问 Tavily `/search` 接口。
- 请求参数中设置 `search_depth`、`include_answer`、`max_results` 等字段。
- 不请求网页全文，只保留标题、URL、短摘要和相关分数。
- 对过长内容进行截断，避免大模型上下文过大导致 JSON 输出中断。

这样既保留了必要证据，又控制了传给大模型的信息长度。

### 6.3 抽取阶段

ExtractorAgent 根据搜索证据生成单个 `CompetitorProfileOutput`，包括：

- 产品名称和官网
- 产品定位
- 目标用户
- 核心功能
- 定价信息
- 优势和劣势
- 用户反馈
- 支撑来源

提示词要求模型在证据不足时写 `unknown`、`null` 或空列表，而不是编造内容。该策略可以让报告中的不确定信息显式暴露出来，便于人工复核。

### 6.4 分析阶段

AnalystAgent 接收三个结构化竞品画像，输出 `ComparisonReportOutput`。这一阶段完成更高层次的综合分析，包括：

- 功能矩阵 `feature_matrix`
- 定价矩阵 `pricing_matrix`
- SWOT 分析 `swot`
- 差异化机会 `opportunities`
- 产品策略建议 `recommendations`
- 来源汇总 `source_summary`
- 风险提示 `risks`

为了避免模型只偏向用户最先输入的产品，提示词中特别要求：所有产品都是并列分析对象，即使其中某些产品是系统搜索补充出来的，也必须同等对待。

### 6.5 报告生成阶段

ReportAgent 将结构化分析结果转写为中文 Markdown 报告。报告必须包含：

- 结论摘要
- 产品概览
- 核心功能对比
- 定价策略对比
- SWOT 分析
- 差异化机会
- 产品策略建议
- 风险提示
- 来源列表

报告要求保留 URL，方便使用者追溯信息来源。最终 Markdown 会在 Streamlit 页面右侧展示，并提供下载按钮。

## 7. 数据结构设计

项目内部使用 `dataclass` 表示业务对象，使用 Pydantic 表示大模型输出对象。

`models.py` 中定义了以下核心结构：

- `Source`：一条资料来源，包含标题、URL、摘要、相关性和来源类型。
- `Pricing`：产品定价信息，使用三态布尔值表示是否有免费版。
- `CompetitorProfile`：单个竞品的结构化画像。
- `AnalysisPlan`：一次分析任务的执行计划。
- `ComparisonReport`：最终报告生成前的结构化中间结果。

`agno_workflow.py` 中又定义了对应的 Pydantic 输出 schema，例如 `AnalysisPlanOutput`、`CompetitorProfileOutput`、`ComparisonReportOutput` 和 `MarkdownReportOutput`。这样设计的原因是：大模型调用阶段需要严格校验 JSON 格式，而项目内部传递时使用 dataclass 更轻量、易序列化。

## 8. 结构化输出与可靠性控制

大模型输出天然具有不稳定性。为了提高系统可靠性，本项目加入了多层控制。

第一，所有 Agent 都设置 `output_schema` 和 `use_json_mode=True`，要求模型返回符合 Pydantic schema 的 JSON。

第二，`_run_structured()` 方法会统一处理 Agno 返回值。如果返回内容已经是目标 schema，则直接使用；如果是字典，则执行 `model_validate()`；如果是字符串，则先去掉可能存在的 Markdown 代码块，再执行 `model_validate_json()`。

第三，规划阶段会检查竞品数量是否恰好为 3；分析阶段会检查 AnalystAgent 返回的竞品是否覆盖 PlannerAgent 规划出的产品。如果不一致，系统会抛出错误，而不是继续生成可能跑偏的报告。

第四，搜索工具主动压缩 Tavily 结果，只传递短摘要，降低上下文过长造成的 JSON 截断风险。

第五，报告中的风险提示用于记录证据不足、信息不确定、来源有限等问题，让用户知道哪些结论需要进一步人工验证。

## 9. 缓存机制

竞品画像抽取会消耗搜索 API 和模型调用。为了减少重复成本，项目提供了可选缓存。

缓存逻辑位于 `cache_tools.py`：

- `slugify()` 将产品名称转换成适合作为文件名的字符串。
- `cache_path()` 根据产品名称生成 JSON 缓存路径。
- `read_cache()` 读取已有竞品画像。
- `write_cache()` 将新的竞品画像写入磁盘。

在 `AgnoCompetitorWorkflow._get_profile()` 中，如果用户开启缓存并命中对应产品，系统会直接从 `data/cache/agno` 读取历史画像，跳过搜索和抽取阶段。这样适合重复分析同一批产品的场景。

## 10. 前端交互实现

前端使用 Streamlit 实现，主要功能包括：

1. 侧栏配置 Tavily API Key、DeepSeek API Key 和模型名。
2. 提供“使用竞品画像缓存”开关。
3. 在主区域输入分析需求。
4. 点击“开始分析”后运行多智能体工作流。
5. 通过 `progress_callback` 实时显示规划、搜索、抽取、分析、报告生成等状态。
6. 在右侧展示生成的 Markdown 报告。
7. 提供 Markdown 文件下载按钮。

API Key 只在网页运行时输入，不写入 `.env`、源码或仓库文件。这种方式降低了密钥泄露风险，也符合课程项目提交代码时的安全要求。

## 11. 安装与运行方式

项目使用 `pyproject.toml` 管理依赖。安装步骤如下：

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -e .
```

启动网页：

```powershell
streamlit run streamlit_app.py
```

打开页面后，需要在侧栏填写 Tavily API Key、DeepSeek API Key 和模型名，然后输入分析需求，例如：

```text
分析 Figma 和它的竞品
```

系统会自动补齐竞品并生成报告。

## 12. 结果验证方法

为了验证项目生成结果是否可信，可以从以下几个方面检查：

1. **结构验证**：观察程序是否能稳定生成包含规定章节的 Markdown 报告。
2. **来源验证**：检查报告来源列表中的 URL 是否真实存在，结论是否能被来源支撑。
3. **竞品范围验证**：检查报告是否始终围绕 3 个并列产品展开，而不是只分析单个产品。
4. **定价和功能验证**：随机打开官网、定价页或产品文档，核对模型抽取的功能和价格信息。
5. **缓存验证**：开启缓存后重复分析同一产品，确认系统可以复用历史画像并减少搜索调用。
6. **异常验证**：故意不填写 API Key 或填写无效 Key，确认系统能给出明确错误提示。

课程报告和演示时，可以选择一个熟悉领域的产品作为样例，例如 ChatGPT、Claude、Gemini，或 Figma、Canva、Miro，然后人工核对报告中的关键事实。

## 13. 项目特点与创新点

本项目的主要特点包括：

- 使用多智能体拆解任务，而不是单次提示词直接生成完整报告。
- 结合 Tavily 搜索真实网页资料，增强结果的事实基础。
- 使用 Pydantic schema 约束中间结果，使流程更稳定、可调试。
- 把竞品画像、分析报告等结果结构化，便于未来扩展到 PPT、PDF 或可视化仪表盘。
- 支持运行时输入 API Key，避免把密钥写入项目文件。
- 提供 Streamlit 可视化页面，降低普通用户使用门槛。

## 14. 局限性与改进方向

当前项目仍有一些可以改进的地方：

1. **来源质量控制仍可增强**：目前主要依赖 Tavily 搜索结果，未来可以优先抓取官网、定价页、产品文档等高可信来源。
2. **缺少自动化测试**：可以补充单元测试，覆盖缓存、搜索结果压缩、schema 校验和工作流异常处理。
3. **报告格式可以继续优化**：未来可以支持一键导出 Word、PDF 或 PPT，更贴近期末提交材料格式。
4. **事实核验可以更严格**：可以增加引用级别的证据映射，让每条关键结论都对应具体 URL。
5. **成本和速度可以优化**：可以并行搜索多个产品，或对搜索结果做更细粒度缓存。
6. **本地模型支持可以前端化**：后端配置中保留了 Ollama 相关参数，未来可以在网页中提供 DeepSeek 和本地模型切换。

## 15. 总结

Competitor Insight Agent 实现了一个完整的大模型应用项目：用户通过 Streamlit 页面提交自然语言需求，系统利用 Agno 编排多个智能体，通过 Tavily 获取真实网页证据，再由 DeepSeek 完成结构化抽取、横向分析和中文报告生成。项目在实现中重点解决了大模型应用常见的三个问题：信息来源不足、输出格式不稳定、结果难以验证。

通过多智能体分工、Pydantic 结构化约束、搜索证据注入和缓存机制，本项目形成了一条较完整的竞品分析自动化流程。它既能作为课程大作业展示大模型应用开发能力，也具备继续扩展为产品分析助手、市场研究工具或企业内部知识分析系统的潜力。
